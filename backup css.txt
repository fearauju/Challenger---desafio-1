for(let i = 0; i < mensagem.length; i++){
        
        if(mensagem[i] == 'a'){
            letraMensagem = mensagem[i];
            padraoCriptografia = letraMensagem.replace(letraMensagem, 'ai');
            mensagemCriptografada = mensagemCriptografada.concat(padraoCriptografia);
            
        }else{
            if(mensagem[i] == 'e'){
                letraMensagem = mensagem[i];
                padraoCriptografia = letraMensagem.replace(letraMensagem, 'enter');
                mensagemCriptografada = mensagemCriptografada.concat(padraoCriptografia);
               
            }else{
                if(mensagem[i] == 'i'){
                    letraMensagem = mensagem[i];
                    padraoCriptografia = letraMensagem.replace(letraMensagem, 'imes');
                    mensagemCriptografada = mensagemCriptografada.concat(padraoCriptografia);
                    
                }else{
                    if(mensagem[i] == 'o'){
                        letraMensagem = mensagem[i];                    
                        padraoCriptografia = letraMensagem.replace(letraMensagem, 'ober');
                        mensagemCriptografada = mensagemCriptografada.concat(padraoCriptografia);
                        
                    }else{
                        if(mensagem[i] == 'u'){
                            letraMensagem = mensagem[i];
                            padraoCriptografia = letraMensagem.replace(letraMensagem, 'ufar');
                            mensagemCriptografada = mensagemCriptografada.concat(padraoCriptografia);                            
                        }else{
                            letraMensagem = mensagem[i];
                            mensagemCriptografada = mensagemCriptografada.concat(letraMensagem);                            
                        }
                    }
                }
            }
        }
}
    @import url('https://fonts.googleapis.com/css2?family=Inter&display=swap');

:root{
    --cor-primaria: #E5E5E5;
    --cor-retangulo: #F6F6F6;
    --corFonte-primaria:#0A3871;

    --fonte-primaria: 'Inter', sans-serif;
}

*{
    padding: 0;
    margin: 0;    
}

body{   
    background-color: var(--cor-primaria);
    min-height: 100vh;    
}

.logotipo{
    margin: 2.5rem 0 0 2.5rem;
}
.paginaCriptografia{
    
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-content: stretch;
    align-items: center;    
}

.paginaCriptografia__areaDigitacao{       
    display: flex;
    flex-direction: column; /* Organiza os elementos verticalmente */
    align-items: center; /* Centraliza os elementos horizontalmente */
}

.areaDigitacao__texto{    
    justify-items: center;
    align-items: center;
    height: 48rem;
    width: 45rem; /* Define a largura da caixa de mensagem */
    top: 2rem;
    bottom: 1rem; 
    padding: 5rem; /* Adiciona espaçamento interno */
    margin: 0 0 0 15rem;
    border: 1px solid var(--corFonte-primaria);            
    background-color: rgba(255, 255, 255, 0.8); /* Adiciona um fundo branco translúcido */
    border-radius: 1rem; /* Arredonda as bordas da mensagem */
    font-family: var(--fonte-primaria);
    font-weight: 400;
    font-size: 2rem;
    line-height: 3rem;

}

.areaDigitacao__posicionarBotoes{
    display: flex;
    gap: 1.5rem;
    margin-top: 1rem;
}

.areaDigitacao__designBotaoCriptografar{
    bottom: 2rem; /* Posiciona o botão na parte inferior */        
    padding: 2rem 6rem; /* Adiciona espaçamento interno */
    margin: 0 0 0 16rem;
    background-color: var(--corFonte-primaria); /* Define a cor de fundo do botão */
    /*font-family: inter*/
    font-size: 1rem;
    color: #FFFF; /* Define a cor do texto do botão */
    border: 1px solid var(--corFonte-primaria);     
    border-radius: 2rem; /* Arredonda as bordas do botão */
    cursor: pointer; /* Define o cursor do mouse como um ponteiro */
    font-family: var(--fonte-primaria);
    font-weight: 400;
    font-size: 2rem;
    line-height: 1.21rem;
}

.areaDigitacao__designBotaoDescriptografar{
    bottom: 2rem; /* Posiciona o botão na parte inferior */        
    padding: 2rem 5rem; /* Adiciona espaçamento interno */    
    background-color:#D8DFE8; /* Define a cor de fundo do botão */    
    font-size: 1rem;
    color: var(--corFonte-primaria); /* Define a cor do texto do botão */
    border: 1px solid var(--corFonte-primaria);     
    border-radius: 2rem; /* Arredonda as bordas do botão */
    cursor: pointer; /* Define o cursor do mouse como um ponteiro */
    font-family: var(--fonte-primaria);
    font-weight: 400;
    font-size: 2rem;
    line-height: 1.21rem;
}

.areaDigitacao__Aviso{
    color: #495057;
    border: 1px solid #D8DFE8;
    margin: 0.5rem 9rem 0 0;   
}
/*------------------------------área descriptografia pronta*/

.paginaCriptografia__areaExibicao{        
    position: relative; /* Define a área de exibição como um contêiner de posicionamento */    
    display: flex;
    flex-direction: column; /* Organiza os elementos verticalmente */
    align-items: center; /* Centraliza os elementos horizontalmente */
       
}
         
.areaExibicao__texto{
    position: absolute; /* Posiciona a mensagem em relação a imagem */    
    top: 2rem; 
    border: 1px solid var(--corFonte-primaria);    
    width: 25rem; /* Define a largura da caixa de mensagem */
    padding: 1rem; /* Adiciona espaçamento interno */
    margin-right: 1.2rem;    
    background-color: rgba(255, 255, 255, 0.8); /* Adiciona um fundo branco translúcido */
    border-radius: 1rem; /* Arredonda as bordas da mensagem */
}

.areaExibicao__imagem{
    position: relative; /* A imagem se torna o contêiner de posicionamento para os outros elementos */
    box-sizing: border-box; /*não permite que outros elementos saiam da area da imagem*/
    width: 30rem;
    height: 70rem;
    margin-right: 2rem;    
    top: 0; /* Posiciona a imagem no topo */
    left: 0; /* Posiciona a imagem à esquerda */
    border-radius: 2rem;
}

.areaExibicao__botao{
    position: absolute; /* Posiciona o botão em relação a imagem*/
    bottom: 2rem; /* Posiciona o botão na parte inferior */        
    padding: 2rem 10rem; /* Adiciona espaçamento interno */
    background-color: var(--cor-primaria); /* Define a cor de fundo do botão */
    font-family: var(--fonte-primaria);
    font-weight: 400;
    font-size: 2rem;
    line-height: 1.21rem;
    color: var(--corFonte-primaria); /* Define a cor do texto do botão */
    border: 1px solid var(--corFonte-primaria);     
    border-radius: 2rem; /* Arredonda as bordas do botão */
    cursor: pointer; /* Define o cursor do mouse como um ponteiro */
}

.rodape{
    margin-top: 2rem;
    border: 1px solid var(--corFonte-primaria);            
    background-color: var(--corFonte-primaria);
    border-radius: 1rem; /* Arredonda as bordas da mensagem */
    font-family: var(--fonte-primaria);
    color: #F6F6F6;
    font-weight: 700;
    font-size: 2rem;
    text-align: center;
    line-height: 3rem; 
}











